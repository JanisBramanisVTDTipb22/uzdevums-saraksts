function sortdata(props) {
    let sortdata = [false, false, false, true, false, false];
    let allTrue = booleanArray.every(props => props === true);
    console.log(allTrue);
    //props.data.map((user) => 
    //)
    return;
}
export default sortdata;